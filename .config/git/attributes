# attributes
# User-specific gitattributes file.  See gitattributes(5) manuals for more
# information.

# Ensure all text files have line endings normalized, i.e., set to LF in
# repository
* text=auto
